-공유 참조 [RefMain1_1]
해당 내용에서는 단순 대입으로 인해 참조값을 공유하는 공유 참조가 발생하여 side effect 발생함을 볼 수 있었다.
여기서 문제는 단순 대입을 한 것이 아니다. 이는 코드 상 전혀 문제가 없다.
문제는 고유된 객체의 값을 변경할 수 있는 것에 있다. -> 불변객체[RefMain2]

- 불변 객체
말 그대로, 어떻게든 객체의 값(필드 값)을 바꾸지 못하게 하면 된다.